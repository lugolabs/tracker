<%= bootstrap_form_for client, remote: true do |form| %>
  <%= form.hidden_field :original_project %>
  <%= form.text_field :name %>
  <%= form.text_area :notes %>

  <div class="mt-4">
    <%= form.submit 'Save', class: 'btn btn-sm btn-primary' %>
  </div>
<% end %>
